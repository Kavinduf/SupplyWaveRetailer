import { StyleSheet, Text, View } from "react-native";
import React from "react";
import { Button, Card } from "@rneui/themed";

const CatergoryCard = ({ onClick, title }) => {
  return (
    <View>
      <Card></Card>
    </View>
  );
};

export default CatergoryCard;

const styles = StyleSheet.create({});
